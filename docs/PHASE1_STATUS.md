# Phase 1 实现状态

> 当前版本是架构验证原型，不是忠实复刻验收版。用户提供的真机试玩视频将作为下一阶段的唯一视觉/行为基准；开发顺序调整为“2D 原子拆解与复刻 → 人类试玩闭环 → Replay/节奏 → 视频导出 → 3D 泛化”。现有 3D 材质与破碎代码保留，但不作为当前质量基线。


## 已落地

- 8×8 方块放置规则、三块候选、同步清行列、得分/Combo、候选刷新、Game Over。
- 18 种基础方块与四套牌面模板。
- 真人拖拽录制：语义落点、归一化指针轨迹、动作帧长、Seed 和状态。
- 基础机器玩家：直接查询合法动作并输出与真人一致的 Take。
- 四套节奏模板，Raw Take 编译为固定帧导演回放。
- Three.js 三维棋盘、真实厚度/倒角、三套物理材质、三套灯光、三套摄像机。
- 三维碎片、GPU 实例化、次级粒子、冲击波、Bloom、镜头冲击。
- 项目 JSON 导入导出；DCC Provider 接口预留。
- Mediabunny + WebCodecs 的 H.264/MP4 逐帧导出实现。
- 核心规则单元测试、无依赖玩法烟雾检查，以及 TypeScript/导入/JSON 源码完整性检查。
- 试玩与渲染期间的状态锁、导出快照冻结、AVC 能力预检与确定性多 Seed 回归检查。

## 明确没有冒充完成的部分

- 当前交付环境无法访问 npm registry，因此未在此环境安装前端依赖，也未完成真实 Chrome GPU 截图和 MP4 编码基准。
- 一期视频只包含画面，不含声音混合。
- 当前编辑器只配置首组三个候选块；后续候选组由 Seed 确定性生成。完整多组序列编辑与自定义形状绘制在 v0.2。
- 机器玩家是规则型贪心 Bot，不是创意规划 Agent。
- 分数公式为独立实现，核心放置/清除机制已实现，但未复制任何第三方私有资源、品牌 UI、音频或内部算法。
- WebGPU 专用后端尚未启用；当前稳定后端是 Three.js WebGL2，接口允许后续切换 WebGPU。

## 下一道工程门禁

在具备 npm 网络和桌面 Chrome 的开发机执行：

```bash
npm install
npm run check
npm test
npm run typecheck
npm run build
npm run dev
```

当前交付环境已经执行并通过 `npm run check`、离线语法检查和严格核心 TypeScript 编译；受 npm registry DNS 隔离影响，完整依赖安装、Vitest、生产构建与 Chrome MP4 真机基准需要在联网开发机完成。

随后完成三项真机基准：连续试玩 P95 帧时间、15 秒 1080×1920 导出耗时、H.264 MP4 播放兼容性。
