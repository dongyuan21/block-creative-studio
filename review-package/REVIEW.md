# REVIEW — T0–T5 / Next phase single-agent execution

本文件与仓库根目录 `REVIEW.md` 同步。Git 内 `frames/`、`videos/` 为 **stale / superseded**。当前 HEAD 的浏览器证据只应来自 `review-package/run/` 与 CI Artifact `capture-run`。
