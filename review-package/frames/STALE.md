# Stale capture frames

These PNG files were produced by an earlier implementation commit (`d785ae8` / `b583806` era) and are **not** visual evidence for the current HEAD.

Current-HEAD capture writes only to `review-package/run/` (gitignored) and CI uploads the `capture-run` artifact. Do not treat files in this directory as the latest review package.
