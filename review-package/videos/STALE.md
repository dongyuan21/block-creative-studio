# Stale capture videos

The MP4 files in this directory are **stale / superseded**. They were not regenerated on the current HEAD. Chrome smoke does not encode videos.

Current-HEAD full capture, when run, writes to `review-package/run/videos/` and is uploaded as the `capture-run` CI artifact. These git binaries remain only as historical samples and are not frame-exact evidence of the current renderer.
