# Experimental gameplay research

This folder preserves the pre-V1 research engine for the observed `direct-set-clear`,
`manual-in-place-match`, goals, and configurable loss variants. It is intentionally
not imported by the production TapTile workspace. The production entry point is the
single deterministic `taptile-tray-match3-v1` engine one directory above.
